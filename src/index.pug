extends layouts/default.pug

block prepend footer
  - var footerEnable = "false"

block prepend head
  - var pageTitle = "Page Meta Title"
  - var pageDescription = "Page Meta Desc"
  - var pageName = "Home"
  - var pageClass = "page-body-class"

block content
  div(class= 'flex items-center justify-center h-screen w-full overflow-hidden relative')
    img(src=dirAssets + 'kiler-anasayfa.jpg' alt='anasayfa' class='w-full h-full object-cover') 
